public Integer fibonacci(Integer n) {
    Integer cur = 1; 
    Integer last = 1; 
    Integer sum = 0;

    for (Integer i = 3; i <= n; i++){
        if (i==1 || i==2) return 1;
        else {
        sum = last + cur;
        cur = last;
        last = sum;
        }
    }
    return sum;
}